<?php
// A config file that returns an array. `$cfg = require 'config.php';` hands the
// returned value straight back to the caller -- a very common PHP pattern.

return [
    'name'    => 'shapes',
    'version' => '1.0',
];
