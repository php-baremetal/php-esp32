# env-demo :: configuration baked into the firmware (see docs/environment.md).
# Edit these and re-run `phpflash build` -- the new values are compiled in.
DEVICE_NAME=p4-lab-01
API_BASE=https://api.example.test/v1
SAMPLE_HZ=5
DEBUG=1
GREETING="hello from flash"
